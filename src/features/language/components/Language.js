import React from "react";
import QuestionLayout from "../../../layouts/QuestionLayout";
import Left from "./Left";

const Language = () => (
  <div>
    <QuestionLayout left={<Left />} />
  </div>
);

export default Language;
